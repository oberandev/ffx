#!/bin/bash

set -euo pipefail

pkg=api

yarn tsc -b packages/${pkg}/tsconfig.lib.json && \
  cp packages/${pkg}/package.json packages/${pkg}/README.md packages/${pkg}/CHANGELOG.md dist/out-tsc/packages/${pkg}/

