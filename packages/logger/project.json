{
  "name": "logger",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/logger/src",
  "projectType": "library",
  "targets": {
    "build": {
      "command": "packages/logger/build"
    },
    "format:check": {
      "command": "prettier ./**/*.{mts,ts} --check"
    },
    "lint": {
      "command": "eslint packages/logger/**/*.{mts,ts}"
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "coverage": true,
        "passWithNoTests": false,
        "reporters": ["json"],
        "reportsDirectory": "../../coverage/packages/logger"
      },
      "configurations": {
        "watch": {
          "watch": true
        }
      }
    }
  },
  "tags": []
}
